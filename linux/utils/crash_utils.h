#ifndef CRASH_UTILS_H_
#define CRASH_UTILS_H_

#include <string>
#include <vector>

namespace crash_utils {
    bool Init();
    bool Init(const std::string &str_crash_path);
} // namespace crash_utils

#endif /* CRASH_UTILS_H_ */
 
 
